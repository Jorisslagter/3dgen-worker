# 3DGen Worker

RunPod serverless worker voor Hunyuan3D-2.1 3D model generatie.

Accepteert een base64-encoded afbeelding, genereert een 3D mesh op H100 GPU,
en retourneert het GLB bestand als base64.
